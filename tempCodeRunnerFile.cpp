vector<int> diff;
    // int maxProfit = 0;
    // // Find local minima
    // int max_occ = INT_MIN;
    // for (int i = 1; i < a.size()-1; i++)
    // {
    // //   if(a[0] > a[1])
    // //   {
    // //       continue;
    // //   }

    //   if(a[i] > a[i-1] && a[i] > a[i+1])
    //   {
    //       int j = i;
    //       int  k = a[i];
    //       while(j < a.size())
    //       {
             
    //           max_occ = max(a[j],k);
    //         //   k = max_occ;
    //           j++;
    //       }

    //       maxProfit += max_occ - a[i]; 
    //   }
    //   else
    //   continue;
    // }