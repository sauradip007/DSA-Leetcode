#include <iostream>
#include<math.h>
#include <cstring>
#include <algorithm>
#include <unordered_map>
#include <vector>
#include <string>
#include <stack>
using namespace std;
int main()
{
    // string a;
    // a.push_back('a');
    // a.push_back('b');
    // a.push_back('c');
    // a.push_back('d');
    // cout<<a<<endl;
    // string password = "!@#$%^&*()-+";
    // string q = "(1Akde";
    // cout<<password.find(q[0])<<endl;
    // string b = "hello";
    // string d = "Hi";
    // int diff = (int)b[0] - (int)d[0];
    // cout<<abs(diff)<<endl;
    // vector<string>exp{"flower","flow","flight"};
    // sort(exp.begin(),exp.end());
    // for(int i = 0 ; i < exp.size() ; i++)
    // {
    //     cout<<exp[i]<<" ";
    // }cout<<endl;

    string u =  "abcd";
  
    // cout<<u.find("ab")<<endl;
    // for (int i = 0 ; i < exp.size() ; i++)
    // {
    //     sort()
    // }

    // cout<<b.find(d[0])<<endl;
    // if(b.find(d) >= 0 && b.find(d) < b.length())
    // {
    //     cout<<b.find(d)<<endl;
    // }
    // else{
    //     cout<<"-1"<<endl;
    // }
    // string t = a;
    // cout<<(int)t[1]<<endl;
    // cout<<(int)'1'<<endl;
    // reverse(a.begin(),a.end());
    // cout<<a<<endl;
    // cout<<d.find("a")<<endl;
    // if(d.substr(1) == b)
    // cout<<"Yes"<<endl;
    // else
    // cout<<"Nah"<<endl;
    // string c = "defgh";

    // cout<<b.substr(0,3)<<endl;

    vector<string>a{"Lamp","Polish","Miner","Stead"};
    a.pop_back();
    for(int i = 0 ; i < a.size(); i++)
    {
        cout<<a[i]<<" ";
    }cout<<endl;

}